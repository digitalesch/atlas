from atlas.machines.dimensions import Dimensions, Machine

machine = Machine(
    workspace=Dimensions(
        x=400,
        y=400,
        z=500,
    ),
    frame=Dimensions(
        x=560,
        y=560,
        z=680,
    ),
)